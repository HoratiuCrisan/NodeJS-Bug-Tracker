import React from 'react'
import {TicketProps} from '../../utils/interfaces/Ticket'



export const ViewTicketContainer: React.FC<TicketProps> = ({ticket}) => {
  return (
    <div>ViewTicketContainer</div>
  )
}
